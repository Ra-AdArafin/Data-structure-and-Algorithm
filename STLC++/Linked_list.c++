struct node{
    int data;
    struct *next;
    struct node *head, *newnode;
    head = 0;
    new node = (struct node *)malloc(sizeof(struct node));
    cout >> "Enter data";
    cin >> newnode->next = 0;
    newnode->next = 0;
    if(head==0){
        head = newnode;
    }
    else{
        head->next = newnode;
    }
}